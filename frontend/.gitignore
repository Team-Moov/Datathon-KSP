# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# .env.example is the committed template; nothing else under this name is,
# except .env.production — it carries only the backend's public origin, and a
# production build without it is silently broken (see that file's comment).
.env
.env.*
!.env.example
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
